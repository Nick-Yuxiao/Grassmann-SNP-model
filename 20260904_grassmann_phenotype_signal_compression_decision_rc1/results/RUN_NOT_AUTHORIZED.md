# RUN NOT AUTHORIZED

_This is a design-only package. No run has been executed and none is authorized here._

---

## 🚫 Why this directory is empty

The decision experiment is frozen (see `COMPRESSION_DECISION_PROTOCOL.md`) but its run code is intentionally not written yet, per the request to produce the design package first. Execution is blocked behind four pre-run gates:

1. the previously flagged centering defect must be fixed and verified;
2. the data contract hashes (frozen v7 chr22 panel manifest + block version) must be bound in `DATA_CONTRACT.json`;
3. the kill margin and CV uncertainty rule must be frozen;
4. explicit project-lead run authorization.

## 📋 What running it will produce (once authorized)

Per-arm R²/AUC-vs-`k` curves and within-block / between-block causal-retention curves on the shared simulated trait panel, followed by evaluation of the two pre-registered kill criteria. Any output placed here after authorization is a decision on the upstream compression question only — never a real-phenotype prediction claim and never an architecture GO/NO-GO beyond the stated kill logic.
