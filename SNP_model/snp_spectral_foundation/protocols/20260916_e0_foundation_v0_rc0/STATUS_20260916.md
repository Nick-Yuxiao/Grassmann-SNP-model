# E0 Foundation-v0 状态报告

_报告时间 2026-09-16 · 里程碑 `R0.5` · 状态 `DEVELOPMENT_VALIDATED_FORMAL_ASSET_BLOCKED` · 固定 cadence `09:00/18:00 America/Los_Angeles`_

---

## 📋 已完成

- 冻结 family × genomic-block 双轴设计与共享 manifest 原则
- 将 LD guard/buffer、long-range-LD atomic block 和训练窗口排除写成验证规则
- 将 `M1a` 局部非神经模型与 `M1b` haplotype-copying/HMM 纳入强基线层级
- 预注册 `1%` relative CE neural-headroom SESOI 与双向 clustered bootstrap
- 将 encoder recipe 预算限制为最多 3 个，并规定 test 只打开一次
- 将 E0 与 phenotype `C vs B` 解耦为平行 estimands
- 核实 SNPBag 官方仓库尚无公开 full-genome pretrained weights
- SNPBag 官方代码仓库已下载到 F 盘，commit `984b8e2af7198005142eeea6f94e848ea2be0d28`；仓库内唯一 `.pt` 为示例 embedding，不是 checkpoint
- 确认当前工作站与已知 WSL 路径均无可访问 UKB genotype/kinship 资产
- 以 1000 Genomes Phase 3 chr22（GRCh37，2,504 人）完成 development-only manifest 演练
- 输入前 100 个变异的 250,400 个 callable GT 均为 phased；该检查只证明输入编码状态，不替代全文件 QC
- 生成 family 与 genomic-block manifests，并以独立验证器通过坐标换算、component 不跨 split、core/guard 隔离和哈希检查
- 开发 split 为 1,753/376/375 人；公开 panel 中相关个体清单与 VCF 样本无交集，因此 2,504 个 component 全为 singleton
- 采用 0.5 cM 开发 blocks、1 cM guard，得到 93 train、22 validation、22 test、5 validation buffer、5 test buffer
- 8 个合成与集成测试全部通过，包括真实多成员 family 不跨 split 和故意泄漏必须失败
- 将 genomic 分配单位从随机散点 block 修正为分层的连续 held-out runs；run 内 LD blocks 仍作为 bootstrap 聚类单位
- 审计出历史 local encoder 的 learned block embedding 不适用于 E0-X，并新增无 block lookup 的 `relative_continuous` 模式
- 冻结 E1 primary、E2 ancestry-conditioned、E3 capacity-sensitivity 三配方上限；模型测试 6/6、protocol 测试 11/11 通过
- 资源盘点确认当前 Windows 与 WSL PyTorch 均为 CPU-only；正式大输出绑定 F 盘（约 189 GB 可用），不使用仅余约 0.46 GB 的 C 盘存放 tensor/checkpoint
- M0 validation-only 两块 dry run 完成：2,829 SNPs、212,589 masks、752 cells；独立 artifact validator `PASS`
- 冻结 M1b 为 Beagle 5.5 `27Feb25.75f` 单 held-out individual adapter；官方资产已下载到 F 盘，jar SHA-256 `7319f4af9638be05c18dcc1bfb8fb41a58a09293507ebf0d54617d0e40df5a70`，Java runtime 尚未安装
- E1 real-VCF train-only smoke `PASS`：1,753 人估计 AF、4 人 × 256 SNP、2 次更新、828,291 parameters；未读取 validation/test、未保存 checkpoint

## ⚠️ 当前阻断

- UKB genotype、kinship、genetic map 与授权路径尚未绑定
- 未生成正式 sample/family/variant/block/mask manifests
- 1000 Genomes 开发 panel 无可用亲缘 component，只验证了 singleton 路径；真实 family split 必须由 UKB kinship 再验证
- 当前 0.5 cM blocks 是工程测试单元，不是 train-derived LD blocks，不能作为正式 E0 证据
- Beagle 软件与版本已选定，但 jar SHA、Java version、概率输出契约和代表性 benchmark blocks 尚未全部绑定
- 未完成 CUDA 环境下的显存/吞吐绑定，正式 E0 不可启动
- GPU 管理工具虽可见约 6 GB 设备，但现有训练环境无 CUDA-enabled PyTorch；micro-batch、accumulation 与 precision 尚不能冻结

## ✍️ 下一次汇报

下一报告为 `R1`，触发条件是 UKB 资产可访问并完成只读 inventory。报告必须给出 cohort/build/phasing、样本数、variant 数、kinship 来源、磁盘路径与哈希计划；在此之前不启动正式训练。开发资产、输入哈希、验证结果与正式阻断条件见 `DEVELOPMENT_REPORT.zh-CN.md`。
