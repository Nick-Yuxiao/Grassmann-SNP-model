# Validation-only 结果与实现确认

_2026-09-18 · `tq_test` 与 `bridge_holdout` 均未打开 · 状态 `IMPLEMENTATION_CONFIRMED`_

---

## 📊 两个已完成 trait 的 validation 扫描

参与者：可用 328,508（338,945 减去 10,437 个 trait 缺失）；train 180,649 / validation 49,307 / tq_test 49,295（未开）。

### `n_30020_0_0` haemoglobin，`r2_A = 0.37737`

| p 阈值 | 位点数 | `r2_B` | `ΔR²` |
| --- | --- | --- | --- |
| 1.0 | 20,801 | 0.35892 | **−0.01844** |
| 0.5 | 11,602 | 0.36028 | −0.01709 |
| 0.1 | 3,348 | 0.37230 | −0.00507 |
| 0.05 | 2,027 | 0.37624 | −0.00113 |
| 0.01 | 749 | 0.38215 | +0.00479 |
| **0.001** | **237** | **0.38396** | **+0.00660** |
| 1e-4 | 118 | 0.38278 | +0.00541 |
| 1e-5 | 72 | 0.38185 | +0.00449 |
| 1e-6 | 54 | 0.38122 | +0.00386 |

### `n_30010_0_0` red blood cell count，`r2_A = 0.26935`

| p 阈值 | 位点数 | `r2_B` | `ΔR²` |
| --- | --- | --- | --- |
| 1.0 | 20,801 | 0.25883 | **−0.01051** |
| 0.5 | 11,675 | 0.25991 | −0.00944 |
| 0.1 | 3,508 | 0.27228 | +0.00293 |
| 0.05 | 2,168 | 0.27566 | +0.00632 |
| 0.01 | 862 | 0.28279 | +0.01345 |
| **0.001** | **336** | **0.28398** | **+0.01463** |
| 1e-4 | 175 | 0.28200 | +0.01265 |
| 1e-5 | 105 | 0.28079 | +0.01144 |
| 1e-6 | 84 | 0.27927 | +0.00992 |

两个 trait 都在 `p < 0.001` 取得最大值，该阈值据此冻结。

## ✅ 四个实现正确性信号

1. **曲线单峰**。松阈值下两万个位点以噪声为主，稀释信号；收紧后噪声退出；过最优点后真信号被丢弃。这正是分数构造应有的形状。
2. **松阈值处 `ΔR²` 为负**。这是最强的一条：样本外 `R²` 能低于以 train 均值为参照的基线，说明它是诚实计算的。若存在泄漏或误算成样本内 `R²`，**不可能出现负值**。
3. **`r2_A` 与 preflight 独立吻合**。HGB `0.37737` 对 preflight 的 `0.376`，RBC `0.26935` 对 `0.273`。差异来自 preflight 在 train+validation 上估计而此处仅用 validation。两条独立代码路径给出一致的协变量模型。
4. **Firewall 完好**。`verdict = VALIDATION_ONLY_TEST_NOT_OPENED`，`splits.test = tq_test`，`splits.never_touched = ["bridge_holdout"]`。

## 📏 量级是否合理

约 2 万个稀疏抽样的 imputed 位点，对遗传度约 0.25–0.35 的血常规指标捕获 0.7%–1.5% 的方差。全 panel、数百万位点的 PRS 通常能到 5%–10%，因此本数量级与「稀疏 panel 的信号下界」这一声明一致。

RBC 高于 HGB 亦可解释：HGB 的 arm A 已达 `0.377`（主要由性别解释），留给基因成分的空间更小。

## 🔮 开 test 之前先写下的预测

**validation 的 `ΔR²` 系统性偏乐观**，因为 p 值阈值正是照着它选出来的。`tq_test` 上的数值预期低于上表。据此预先记录：

- `n_30010_0_0`（RBC，validation `+0.0146`）预期在 test 上仍明显为正，且大概率越过 `0.005` 的 SESOI → `QUALIFIED`
- `n_30020_0_0`（HGB，validation `+0.0066`）距 SESOI 仅 `0.0016`，**回落后落在 SESOI 之下的可能性不小** → 可能为 `ELIGIBLE_PRACTICALLY_TIED`

两者都预期满足 eligibility 本身（`ΔR² > 0` 且 paired 95% CI 下界 `> 0`），因为 `n = 49,295` 下这一量级的效应区间应稳定排除零。

**此预测写于 test 打开之前，用于事后对照，不构成对判定规则的任何修改。** 无论结果如何，SESOI 与 eligibility 规则均不调整。

## ⏭️ 尚未完成

`n_30040_0_0`（MCV）的 validation 仍在运行。三个 trait 齐备后，仍须先解决撤回名单缺口，才能打开 `tq_test`。
